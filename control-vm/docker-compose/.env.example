# ============================================================================
# Control VM Services - Environment Variables
# ============================================================================
# SECURITY: This is a TEMPLATE file. DO NOT commit .env with real secrets!
# Usage: cp .env.example .env && edit .env with actual values
# ============================================================================

# ----------------------------------------------------------------------------
# Semaphore Configuration - Ansible Web UI
# ----------------------------------------------------------------------------

# PostgreSQL Database
SEMAPHORE_DB_NAME=semaphore
SEMAPHORE_DB_USER=CHANGEME_db_username
SEMAPHORE_DB_PASSWORD=CHANGEME_db_password

# Semaphore Admin Account
SEMAPHORE_ADMIN_NAME=CHANGEME_admin_username
SEMAPHORE_ADMIN_EMAIL=admin@localhost
SEMAPHORE_ADMIN_PASSWORD=CHANGEME_admin_password

# Semaphore Access Key Encryption (used for encrypting credentials)
# Generate with: openssl rand -hex 32
SEMAPHORE_ACCESS_KEY_ENCRYPTION=CHANGEME_generate_with_openssl_rand_hex_32

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# Vault: No env vars needed - configuration in vault-config.hcl
# Registry: Authentication via htpasswd file (see setup script)
# MkDocs: No configuration needed
# Portainer: Configuration via web UI on first login
# Semaphore: Access at http://<control-vm-ip>:3000
